<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="https://github.com/spotbugs/filter/3.0.0"
        xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd">

    <!-- Exclude EI_EXPOSE_REP and EI_EXPOSE_REP2 (exposing internal mutable state/representation) -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2"/>
    </Match>

    <!-- Exclude CT_CONSTRUCTOR_THROW (constructor throws exception, serialization susceptibility) -->
    <Match>
        <Bug pattern="CT_CONSTRUCTOR_THROW"/>
    </Match>

    <!-- Exclude DCN_NULLPOINTER_EXCEPTION (catching NullPointerException) -->
    <Match>
        <Bug pattern="DCN_NULLPOINTER_EXCEPTION"/>
    </Match>

    <!-- Exclude RV_RETURN_VALUE_IGNORED (ignored method return values, common in unit tests) -->
    <Match>
        <Bug pattern="RV_RETURN_VALUE_IGNORED"/>
    </Match>

</FindBugsFilter>
